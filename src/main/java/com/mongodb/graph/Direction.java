package com.mongodb.graph;

/*
 * Enumeration used to specify directions that are allowed
 * during a graph search operation and also specify which
 * direction a Relationship exists in.
 */
public enum Direction {
	IN,
	OUT,
	BOTH

}
